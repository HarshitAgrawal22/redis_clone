TODO > 1 : Create a CI/CD pipeline for code (Done)
TODO > 3 : segregate send message function from peer to network function (Done)
TODO > 4 : implement resp all the way in project 
TODO > 5 : learn docker and host that on python anywhere or any other platform (Done)
TODO > 6 : Update readme.md by a lot better things
TODO > 7 : Add Global Exception to the project (Done)
TODO > 8 : Write test cases

python3 -m venv venv
source venv/bin/activate


| Your Constant        | RESP Prefix | Format                      | Example (Raw RESP)                   | Human Meaning           | Used For          |
| -------------------- | ----------- | --------------------------- | ------------------------------------ | ----------------------- | ----------------- |
| `error`              | `-`         | `-<message>\r\n`            | `-ERR unknown command\r\n`           | Error message           | Command failures  |
| `simple_string`      | `+`         | `+<string>\r\n`             | `+OK\r\n`                            | Simple success response | Acknowledgement   |
| `bulk_string`        | `$`         | `$<length>\r\n<string>\r\n` | `$5\r\nhello\r\n`                    | Binary-safe string      | Actual values     |
| `bulk_string (null)` | `$`         | `$-1\r\n`                   | `$-1\r\n`                            | Null value              | Missing key       |
| `integer`            | `:`         | `:<number>\r\n`             | `:100\r\n`                           | Integer number          | Counters, lengths |
| `array`              | `*`         | `*<count>\r\n<elements>`    | `*2\r\n$4\r\nHGET\r\n$4\r\nname\r\n` | List of elements        | Commands, lists   |
| `array (empty)`      | `*`         | `*0\r\n`                    | `*0\r\n`                             | Empty list              | Empty result      |
| `array (null)`       | `*`         | `*-1\r\n`                   | `*-1\r\n`                            | Null array              | Missing data      |
telnet switchback.proxy.rlwy.net 14454






Verified 

Keyval:-
    setm 
    set
    get
    getm 
    HLEN
    hello
    check
    client
    hdel
    hincryby
    hgetattr 
    hsetttr
    kill
    shaanti

liststruc: all are working fine

queue: all are working fine

sets: all are working fine

stack: all are working fine 

tree: 
    "tins"  ok 
    "tset"  ok
    "tget"  ok 
    "tlook" ok 
    "tdel"  ok
    "tpre"  ok
    "tpost" ok
    "tin"   ok
    "tshow" ok 
    "tusr"  
